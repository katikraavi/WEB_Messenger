                                     Table "public.users"
       Column        |            Type             | Collation | Nullable |      Default      
---------------------+-----------------------------+-----------+----------+-------------------
 id                  | uuid                        |           | not null | gen_random_uuid()
 email               | text                        |           | not null | 
 username            | text                        |           | not null | 
 password_hash       | text                        |           | not null | 
 email_verified      | boolean                     |           |          | false
 profile_picture_url | text                        |           |          | 
 about_me            | text                        |           |          | 
 created_at          | timestamp without time zone |           |          | now()
Indexes:
    "users_pkey" PRIMARY KEY, btree (id)
    "idx_user_email_lower" btree (lower(email))
    "idx_user_is_verified" btree (email_verified)
    "idx_user_username_lower" btree (lower(username))
    "idx_users_email" btree (email)
    "idx_users_username" btree (username)
    "users_email_key" UNIQUE CONSTRAINT, btree (email)
    "users_username_key" UNIQUE CONSTRAINT, btree (username)
Referenced by:
    TABLE "chat_members" CONSTRAINT "chat_members_user_id_fkey" FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
    TABLE "invites" CONSTRAINT "invites_receiver_id_fkey" FOREIGN KEY (receiver_id) REFERENCES users(id) ON DELETE CASCADE
    TABLE "invites" CONSTRAINT "invites_sender_id_fkey" FOREIGN KEY (sender_id) REFERENCES users(id) ON DELETE CASCADE
    TABLE "messages" CONSTRAINT "messages_sender_id_fkey" FOREIGN KEY (sender_id) REFERENCES users(id) ON DELETE RESTRICT


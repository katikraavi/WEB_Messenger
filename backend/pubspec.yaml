name: messenger_server
description: Backend server for Mobile Messenger application
publish_to: none
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  postgres: ^2.0.0
  cryptography: ^2.1.0
  dotenv: ^4.0.0
  # Note: serverpod ^3.4.2 will be added when Serverpod CLI is properly configured
  # Currently using shelf for development server

dev_dependencies:
  test: ^1.21.0
  lints: ^3.0.0
